<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.hms.scankit">

    <!--permission for WiFI post processing,not for scankit itself-->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"></uses-permission>
    <!--camera permission-->
    <uses-permission android:name="android.permission.CAMERA" />
    <!--read permission for Bitmap Mode-->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!--write permission for save QRCODE Bitmap,not for scankit itself-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-permission android:name="android.permission.VIBRATE" />

        <application>

            <activity android:name="com.huawei.hms.hmsscankit.ScanKitActivity" />
            <activity android:name=".ScanKitDemoActivity"/>
            <activity android:name=".BasicScanActivity"/>
            <activity android:name=".CustomizsedViewActivity"/>
            <activity android:name=".multiprocessor.CommonActivity"/>
            <activity android:name=".GenerateCodeActivity"/>

        </application>
    </manifest>